(function () {
  function report(fn) {
    fn.
  }

  var GameRouter = function (numOfGames) {
    // instantiate games.
    // stitch them togther with appropriate reference passing.
    // synchronize steps.

  };
})();